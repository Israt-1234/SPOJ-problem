#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
  int n,d;
  scanf("%d %d",&n, &d);
  while (d--)
  {
   for(int i=1;i<=n;i++)
  printf("%d ",i);
  printf("\n");
  }
    return 0;
}
